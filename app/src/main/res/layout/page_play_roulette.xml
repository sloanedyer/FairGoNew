<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/background_landscape">

    <include
        android:id="@+id/include"
        layout="@layout/part_page_game" />

    <ImageView
        android:id="@+id/image_rouletteBody"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:layout_marginTop="56dp"
        android:layout_marginEnd="60dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/roulette_circle"
        tools:ignore="ContentDescription" />

    <ImageView
        android:layout_width="20dp"
        android:layout_height="21dp"
        app:layout_constraintBottom_toBottomOf="@+id/image_rouletteBody"
        app:layout_constraintEnd_toEndOf="@+id/image_rouletteBody"
        app:layout_constraintStart_toStartOf="@+id/image_rouletteBody"
        app:layout_constraintTop_toTopOf="@+id/image_rouletteBody"
        app:layout_constraintVertical_bias="0.055"
        app:srcCompat="@drawable/roulette_arrow"
        tools:ignore="ContentDescription" />


</androidx.constraintlayout.widget.ConstraintLayout>